diff --git a/Config.py b/Config.py
index 3d396d5..ff36beb 100644
--- a/Config.py
+++ b/Config.py
@@ -1,8 +1,19 @@
+import sys
+from pathlib import Path
+
+
+PARENT_DIR = Path(__file__).resolve().parent.parent
+if str(PARENT_DIR) not in sys.path:
+    sys.path.insert(0, str(PARENT_DIR))
+
+from common_experiment import SharedExperiment
+
+
 class Config(object):
     
     # System setup
-    N_UE             = 20                   # Number of Mobile Devices
-    N_EDGE           = 2                    # Number of Edge Servers
+    N_UE             = SharedExperiment.NUM_USERS
+    N_EDGE           = SharedExperiment.NUM_EDGES
     UE_COMP_CAP      = 2.6                  # Mobile Device Computation Capacity
     UE_TRAN_CAP      = 14                   # Mobile Device Transmission Capacity
     EDGE_COMP_CAP    = 42                   # Edge Servers Computation Capacity
@@ -22,15 +33,15 @@ class Config(object):
 
     TASK_MIN_SIZE    = 1
     TASK_MAX_SIZE    = 7
-    N_COMPONENT      = 1                    # Number of Task Partitions
-    MAX_DELAY        = 10
+    N_COMPONENT      = SharedExperiment.NUM_COMPONENTS
+    MAX_DELAY        = SharedExperiment.MAX_DELAY
 
 
     # Simulation scenario
-    N_EPISODE        = 1000                 # Number of Episodes
-    N_TIME_SLOT      = 100                  # Number of Time Slots
+    N_EPISODE        = SharedExperiment.QECO_EPISODES
+    N_TIME_SLOT      = SharedExperiment.QECO_TIME_SLOTS
     DURATION         = 0.1                  # Time Slot Duration
-    TASK_ARRIVE_PROB = 0.3                  # Task Generation Probability
+    TASK_ARRIVE_PROB = SharedExperiment.QECO_TASK_ARRIVE_PROB
     N_TIME = N_TIME_SLOT + MAX_DELAY
 
 
@@ -40,4 +51,3 @@ class Config(object):
     E_GREEDY         = 0.99
     N_NETWORK_UPDATE = 200                  # Networks Parameter Replace
     MEMORY_SIZE      = 500                  # Replay Buffer Memory Size
-
diff --git a/MEC_Env.py b/MEC_Env.py
index 8da8fa3..892c50e 100644
--- a/MEC_Env.py
+++ b/MEC_Env.py
@@ -397,17 +397,18 @@ class MEC:
                         #self.TASK_log[ue_index][self.edge_process_task[ue_index][edge_index]['TASK_ID']]['state'] = 2
 
                 # OTHER INFO
-                    if self.edge_ue_m[edge_index] != 0:
-                        self.b_edge_comp[ue_index, edge_index] \
-                            = np.max([self.b_edge_comp[ue_index, edge_index]
-                                        - self.comp_cap_edge[edge_index]/ ue_arrive_task_dens / self.edge_ue_m[edge_index]
-                                        - self.edge_drop[ue_index, edge_index], 0])
+                    if self.edge_ue_m[edge_index] != 0 and ue_arrive_task_dens != 0:
+                        self.b_edge_comp[ue_index, edge_index] \
+                            = np.max([self.b_edge_comp[ue_index, edge_index]
+                                        - self.comp_cap_edge[edge_index]/ ue_arrive_task_dens / self.edge_ue_m[edge_index]
+                                        - self.edge_drop[ue_index, edge_index], 0])
 
         # TRANSMISSION QUEUE UPDATE ===================
         for ue_index in range(self.n_ue):
             #ue_tran_cap = np.squeeze(self.tran_cap_ue[ue_index,:])[1]/self.n_cycle
 
-            ue_tran_cap = np.squeeze(self.tran_cap_ue[ue_index,:])
+            # Keep transmission capacity indexable even when there is only one edge.
+            ue_tran_cap = np.atleast_1d(self.tran_cap_ue[ue_index, :]).astype(float)
             ue_arrive_task_size = np.squeeze(self.arrive_task_size[self.time_count, ue_index])
             ue_arrive_task_dens = np.squeeze(self.arrive_task_dens[self.time_count, ue_index])
         
@@ -570,4 +571,4 @@ class MEC:
                         self.process_delay[self.task_history[ue][task]['TIME'], ue] = self.max_delay
                         self.unfinish_task[self.task_history[ue][task]['TIME'], ue] = 1
         '''
-   
\ No newline at end of file
+   
diff --git a/main.py b/main.py
index c0b92fc..386fcf9 100644
--- a/main.py
+++ b/main.py
@@ -1,19 +1,38 @@
-from MEC_Env import MEC
-from D3QN import DuelingDoubleDeepQNetwork
-from Config import Config
-import matplotlib.pyplot as plt
-import numpy as np
-import random
-import os
-import shutil
-
-'''
-if not os.path.exists("models"):
-    os.mkdir("models")
-else:
-    shutil.rmtree("models")
-    os.mkdir("models")
-'''
+import sys
+from pathlib import Path
+
+PARENT_DIR = Path(__file__).resolve().parent.parent
+if str(PARENT_DIR) not in sys.path:
+    sys.path.insert(0, str(PARENT_DIR))
+
+from MEC_Env import MEC
+from D3QN import DuelingDoubleDeepQNetwork
+from Config import Config
+from common_experiment import (
+    SharedExperiment,
+    apply_global_seed,
+    create_run_dir,
+    resample_series,
+    write_experiment_snapshot,
+)
+import matplotlib.pyplot as plt
+import numpy as np
+import random
+import os
+import shutil
+import tensorflow.compat.v1 as tf
+
+BASE_DIR = os.path.dirname(os.path.abspath(__file__))
+RUN_DIR = BASE_DIR
+MODELS_DIR = os.path.join(RUN_DIR, "models")
+
+'''
+if not os.path.exists(MODELS_DIR):
+    os.mkdir(MODELS_DIR)
+else:
+    shutil.rmtree(MODELS_DIR)
+    os.mkdir(MODELS_DIR)
+'''
 
 
 
@@ -78,11 +97,33 @@ def Cal_Delay(ue_RL_list, episode):
     avg_delay_in_episode = (sum(avg_delay_in_episode)/len(avg_delay_in_episode))
     return avg_delay_in_episode
 
-def Cal_Energy(ue_RL_list, episode):
-    energy_ue_list = [sum(ue_RL.energy_store[episode]) for ue_RL in ue_RL_list]
-    avg_energy_in_episode = sum(energy_ue_list) / len(energy_ue_list)
-    #print(f"energy: {avg_energy_in_episode}")
-    return avg_energy_in_episode
+def Cal_Energy(ue_RL_list, episode):
+    energy_ue_list = [sum(ue_RL.energy_store[episode]) for ue_RL in ue_RL_list]
+    avg_energy_in_episode = sum(energy_ue_list) / len(energy_ue_list)
+    #print(f"energy: {avg_energy_in_episode}")
+    return avg_energy_in_episode
+
+
+def save_series(values, path):
+    with open(path, "w") as f:
+        for value in values:
+            f.write(f"{value}\n")
+
+
+def save_comparison_ready(avg_qoe_list, avg_delay_list, energy_cons_list, num_drop_list):
+    comparison_dir = Path(RUN_DIR) / "comparison_ready"
+    comparison_dir.mkdir(parents=True, exist_ok=True)
+
+    comparison_qoe = resample_series(avg_qoe_list, SharedExperiment.COMPARISON_POINTS)
+    comparison_delay = resample_series(avg_delay_list, SharedExperiment.COMPARISON_POINTS)
+    comparison_energy = resample_series(energy_cons_list, SharedExperiment.COMPARISON_POINTS)
+    comparison_drop = resample_series(num_drop_list, SharedExperiment.COMPARISON_POINTS)
+
+    save_series(comparison_qoe, comparison_dir / "QoE.txt")
+    save_series(comparison_delay, comparison_dir / "Delay.txt")
+    save_series(comparison_energy, comparison_dir / "Energy.txt")
+    save_series(comparison_drop, comparison_dir / "Drop.txt")
+    save_series(np.arange(1, SharedExperiment.COMPARISON_POINTS + 1), comparison_dir / "ComparisonStep.txt")
 
 '''
 def cal_reward(ue_RL_list):
@@ -103,7 +144,7 @@ def cal_reward(ue_RL_list):
 
 '''
 
-def train(ue_RL_list, NUM_EPISODE):
+def train(ue_RL_list, NUM_EPISODE):
     avg_QoE_list = []
     avg_delay_list = []
     energy_cons_list = []
@@ -324,20 +365,19 @@ def train(ue_RL_list, NUM_EPISODE):
                 for ue in range(env.n_ue):
                     ue_RL_list[ue].learn()
 
-            # GAME ENDS
-
-            if done:
-                with open("Delay.txt", 'a') as f:
-                            f.write('\n' + str(Cal_Delay(ue_RL_list, episode)))
-
-                with open("Energy.txt", 'a') as f:
-                            f.write('\n' + str(Cal_Energy(ue_RL_list, episode)))
-
-                with open("QoE.txt", 'a') as f:
-                            f.write('\n' + str(Cal_QoE(ue_RL_list, episode)))
-
-                with open("Drop.txt", 'a') as f:
-                            f.write('\n' + str(Drop_Count(ue_RL_list, episode)))
+            # GAME ENDS
+            if done:
+                with open(os.path.join(RUN_DIR, "Delay.txt"), 'a') as f:
+                    f.write('\n' + str(Cal_Delay(ue_RL_list, episode)))
+
+                with open(os.path.join(RUN_DIR, "Energy.txt"), 'a') as f:
+                    f.write('\n' + str(Cal_Energy(ue_RL_list, episode)))
+
+                with open(os.path.join(RUN_DIR, "QoE.txt"), 'a') as f:
+                    f.write('\n' + str(Cal_QoE(ue_RL_list, episode)))
+
+                with open(os.path.join(RUN_DIR, "Drop.txt"), 'a') as f:
+                    f.write('\n' + str(Drop_Count(ue_RL_list, episode)))
 
 
 
@@ -374,18 +414,32 @@ def train(ue_RL_list, NUM_EPISODE):
                 '''
 
                 
-                if episode % 200 == 0 and episode != 0:
-                    os.mkdir("models" + "/" + str(episode))
-                    for ue in range(env.n_ue):
-                        ue_RL_list[ue].saver.save(ue_RL_list[ue].sess, "models/" + str(episode) +'/'+ str(ue) + "_X_model" +'/model.ckpt', global_step=episode)
-                        print("UE", ue, "Network_model_seved\n")
-                
-                
-                if episode % 999 == 0 and episode != 0:
-                    os.mkdir("models" + "/" + str(episode))
-                    for ue in range(env.n_ue):
-                        ue_RL_list[ue].saver.save(ue_RL_list[ue].sess, "models/" + str(episode) +'/'+ str(ue) + "_X_model" +'/model.ckpt', global_step=episode)
-                        print("UE", ue, "Network_model_seved\n")
+                if episode % 200 == 0 and episode != 0:
+                    episode_dir = os.path.join(MODELS_DIR, str(episode))
+                    os.makedirs(episode_dir, exist_ok=True)
+                    for ue in range(env.n_ue):
+                        model_dir = os.path.join(episode_dir, str(ue) + "_X_model")
+                        os.makedirs(model_dir, exist_ok=True)
+                        ue_RL_list[ue].saver.save(
+                            ue_RL_list[ue].sess,
+                            os.path.join(model_dir, "model.ckpt"),
+                            global_step=episode,
+                        )
+                        print("UE", ue, "Network_model_seved\n")
+                
+                
+                if episode % 999 == 0 and episode != 0:
+                    episode_dir = os.path.join(MODELS_DIR, str(episode))
+                    os.makedirs(episode_dir, exist_ok=True)
+                    for ue in range(env.n_ue):
+                        model_dir = os.path.join(episode_dir, str(ue) + "_X_model")
+                        os.makedirs(model_dir, exist_ok=True)
+                        ue_RL_list[ue].saver.save(
+                            ue_RL_list[ue].sess,
+                            os.path.join(model_dir, "model.ckpt"),
+                            global_step=episode,
+                        )
+                        print("UE", ue, "Network_model_seved\n")
 
 
                     
@@ -465,7 +519,7 @@ def train(ue_RL_list, NUM_EPISODE):
                     # Save the figure to a file
                     plt.tight_layout()
                     plt.subplots_adjust(top=0.9)
-                    plt.savefig('Performance_Chart.png', dpi=100)
+                    plt.savefig(os.path.join(RUN_DIR, 'Performance_Chart.png'), dpi=100)
                     #plt.show()
 
 
@@ -484,13 +538,33 @@ def train(ue_RL_list, NUM_EPISODE):
                 #print("Trans_Drop: ", env.drop_trans_count, "Edge_Drop: ", env.drop_edge_count, "UE_Drop: ", env.drop_ue_count)
                 #print("Drop_Count: ",Drop_Count(ue_RL_list, episode))
 
-                break # Training Finished
-
-
-if __name__ == "__main__":
-
-    # GENERATE ENVIRONMENT
-    env = MEC(Config.N_UE, Config.N_EDGE, Config.N_TIME, Config.N_COMPONENT, Config.MAX_DELAY)
+                break # Training Finished
+
+    save_comparison_ready(avg_QoE_list, avg_delay_list, energy_cons_list, num_drop_list)
+    return {
+        "qoe": avg_QoE_list,
+        "delay": avg_delay_list,
+        "energy": energy_cons_list,
+        "drop": num_drop_list,
+    }
+
+
+if __name__ == "__main__":
+    RUN_DIR = str(create_run_dir("qeco"))
+    MODELS_DIR = os.path.join(RUN_DIR, "models")
+    os.makedirs(MODELS_DIR, exist_ok=True)
+    apply_global_seed(SharedExperiment.RANDOM_SEED, use_tensorflow=True)
+    write_experiment_snapshot(
+        Path(RUN_DIR),
+        extra={
+            "algorithm": "QECO",
+            "models_dir": MODELS_DIR,
+        },
+    )
+    print("Run directory:", RUN_DIR)
+
+    # GENERATE ENVIRONMENT
+    env = MEC(Config.N_UE, Config.N_EDGE, Config.N_TIME, Config.N_COMPONENT, Config.MAX_DELAY)
 
     # GENERATE MULTIPLE CLASSES FOR RL
     ue_RL_list = list()
@@ -512,13 +586,14 @@ if __name__ == "__main__":
         ue_RL_list[ue].epsilon = 1
     '''
 
-    Delay  = open("Delay.txt" , 'w')
-    Energy = open("Energy.txt", 'w')
-    QoE    = open("QoE.txt"   , 'w')
-    Drop   = open("Drop.txt"  , 'w')
+    tf.set_random_seed(SharedExperiment.RANDOM_SEED)
+    Delay  = open(os.path.join(RUN_DIR, "Delay.txt") , 'w')
+    Energy = open(os.path.join(RUN_DIR, "Energy.txt"), 'w')
+    QoE    = open(os.path.join(RUN_DIR, "QoE.txt")   , 'w')
+    Drop   = open(os.path.join(RUN_DIR, "Drop.txt")  , 'w')
                            
 
     # TRAIN THE SYSTEM
-    train(ue_RL_list, Config.N_EPISODE)
+    train(ue_RL_list, Config.N_EPISODE)
 
 
